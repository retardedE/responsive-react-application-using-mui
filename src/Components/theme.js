import { createTheme } from "@mui/material";
import { grey, purple, common, blue } from "@mui/material/colors";
export const theme = createTheme({
  palette: {
    primary: {
      main: blue[900],
      hoverFx: '#f5f5f5',
    },
  },
});
